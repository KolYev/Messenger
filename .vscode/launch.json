{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Messenger (SFML, MSVC)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\messenger.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [
                {
                    "name": "PATH",
                    "value": "${workspaceFolder}\\Libraries\\SFML-3.0.2\\bin;${env:PATH}"
                }
            ],
            "externalConsole": true,
            "preLaunchTask": "build Messenger (SFML, MSVC)"
        }
    ]
}
